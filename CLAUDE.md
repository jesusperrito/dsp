# Times of the Wild — DSP Newspaper

## Design Context

Read [PRODUCT.md](PRODUCT.md) and [DESIGN.md](DESIGN.md) before making any design or UI change in this project.

- **PRODUCT.md**: register (`brand`), platform (`web`), audience, positioning, and the "show don't tell" pitch strategy behind this piece.
- **DESIGN.md**: the visual system — palette (sourced from Dubai Safari Park's real brand deck), typography, elevation, and components. Named rules like *The No-Magenta Rule* and *The No-Blur Rule* are load-bearing; don't violate them.

`/impeccable` commands (`craft`, `critique`, `audit`, `polish`, etc.) read both files automatically.
